import { Hello, HelloModel } from "./Hello";
import { User, UserModel } from "./Users";
import { Post, PostModel } from "./Post";

export const models = { Hello, HelloModel, User, UserModel, Post, PostModel };
